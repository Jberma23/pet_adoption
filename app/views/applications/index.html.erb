<h1>All Applications</h1>
<ul>
    <% @applications.each do |application| %>
      <li>  <%= link_to "#{application.id} - #{application.applicant.first_name} adopting #{application.pet.name}", application_path(application) %> </li>
    <% end %>
</ul>
